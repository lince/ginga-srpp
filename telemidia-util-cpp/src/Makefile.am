
# set the include path found by configure
INCLUDES = $(all_includes)

LIBS =

localLibdir = /usr/local/lib/ginga

libtelemidiautil_so_LDFLAGS = -Wl,-soname,libtelemidiautil.so -shared
localLib_PROGRAMS = libtelemidiautil.so
libtelemidiautil_so_SOURCES = Color.cpp functions.cpp
